// IMPORT ACTIONS

// INTIAL STATE
const initialState = {
	data: 0,
};
// WHAT ACTIONS MUST ACT ON IN THE STATE

export default (state = initialState, action = {}) => {
	switch (action.type) {
		default:
			return state;
	}
};
